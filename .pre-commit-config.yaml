repos:
  - repo: local
    hooks:
      - id: export-requirements
        name: export requirements.txt from poetry.lock
        entry: scripts/export-requirements.sh
        language: script
        files: ^(pyproject\.toml|poetry\.lock)$
        pass_filenames: false
